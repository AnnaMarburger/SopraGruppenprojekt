package messages.data;

/**
 * This enum represents all different kinds of change reasons for map change demands.
 */
public enum ChangeReason {
  SANDSTORM,
  ROUND_PHASE,
  ENDGAME,
  FAMILY_ATOMICS,
  SPICE_PICKUP
}
